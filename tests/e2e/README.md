# e2e tests

## Requirements

- pnpm with node.js

Copy the `.env.example` into `.env` and fill variables before running the tests,
then run the tests like this:

```bash
pnpm playwright test -j 1 --headed
```
