name: Build Khazina

on:
  push:
    branches: [main]
    paths:
      - 'khazina/**'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yaml
    secrets: inherit
    with:
      image_name: khazina
      context_path: ./khazina

  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yaml
    secrets: inherit
